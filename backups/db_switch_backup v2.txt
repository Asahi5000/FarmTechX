    h2 {
      color: green;
      text-align: left;
        margin-bottom: 20px;
    }

    h3 {
      text-align: center;
    }
/* Pump Status Switch */
.status-switch {
  display: flex;
  align-items: center;
  justify-content: center; /* centered */
  margin-bottom: 20px;
}

.switch {
  position: relative;
  display: inline-block;
  width: 50px;
  height: 28px;
}

.switch input {
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  top: 0; left: 0;
  right: 0; bottom: 0;
  background-color: #ccc;
  transition: 0.4s;
  border-radius: 34px;
}

.slider:before {
  position: absolute;
  content: "";
  height: 20px;
  width: 20px;
  left: 4px;
  bottom: 4px;
  background-color: white;
  transition: 0.4s;
  border-radius: 50%;
}

input:checked + .slider {
  background-color: green; /* theme green */
}

input:checked + .slider:before {
  transform: translateX(22px);
}

.status-label {
  margin-left: 10px;
  font-weight: bold;
  color: green; /* theme green */
}

/* Automation Form */
.automation-form {
  display: flex;
  flex-direction: column;
  gap: 15px;
  margin-bottom: 30px;
}

.form-group {
  display: flex;
  flex-direction: column;
}

.form-group label {
  font-weight: bold;
  margin-bottom: 5px;
  color: green; /* theme green */
}

.form-group select,
.form-group input {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 14px;
}

.time-select {
  display: flex;
  gap: 10px;
}

.time-select select {
  padding: 6px;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
}


button {
  background: green; /* theme green */
  color: #fff;
  padding: 12px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 16px;
  transition: 0.3s;
}

button:hover {
  background: #155e32;
}

/* Schedule List */
.schedule-list {
  margin-top: 20px;
}

table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
  font-size: 14px;
}

table th, table td {
  border: 1px solid #ddd;
  padding: 12px;
  text-align: center;
}

table th {
  background: green; /* theme green */
  color: #fff;
}

table td {
  background: #f9f9f9;
}

table td button {
  padding: 6px 12px;
  font-size: 12px;
  margin: 0 2px;
  border-radius: 4px;
}

table td button.edit {
  background: #f4a261;
}

table td button.delete {
  background: #e63946;
}

@media (max-width: 768px) {
  .form-group {
    flex-direction: column;
  }

  table, thead, tbody, th, td, tr {
    display: block;
  }

  table th {
    display: none;
  }

  table td {
    text-align: right;
    padding-left: 50%;
    position: relative;
  }

  table td::before {
    content: attr(data-label);
    position: absolute;
    left: 10px;
    font-weight: bold;
  }
}